name: Deploy Nexora Engine

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          chmod +x ~/server/scripts/deploy.sh
          ~/server/scripts/deploy.sh
